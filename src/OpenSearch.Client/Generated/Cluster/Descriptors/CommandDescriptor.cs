// <auto-generated/>
#nullable enable

using System.Text.Json.Serialization;

namespace OpenSearch.Client.Cluster;

public sealed class CommandDescriptor
{
	internal Command _value = new();

	/// <summary>Cancel allocation of a shard (or recovery). Accepts `index` and `shard` for index name and shard number, and `node` for the node to cancel the shard allocation on. This can be used to force resynchronization of existing replicas from the primary shard by cancelling them and allowing them to be reinitialized through the standard recovery process. By default only replica shard allocations can be cancelled. If it is necessary to cancel the allocation of a primary shard then the `allow_primary` flag must also be included in the request.</summary>
		public CommandDescriptor Cancel(CommandCancelAction? value) { _value.Cancel = value; return this; }
	/// <summary>Cancel allocation of a shard (or recovery). Accepts `index` and `shard` for index name and shard number, and `node` for the node to cancel the shard allocation on. This can be used to force resynchronization of existing replicas from the primary shard by cancelling them and allowing them to be reinitialized through the standard recovery process. By default only replica shard allocations can be cancelled. If it is necessary to cancel the allocation of a primary shard then the `allow_primary` flag must also be included in the request.</summary>
		public CommandDescriptor Cancel(Action<CommandCancelActionDescriptor> configure)
	{
		var descriptor = new CommandCancelActionDescriptor();
		configure(descriptor);
		_value.Cancel = descriptor;
		return this;
	}
	/// <summary>Move a started shard from one node to another node. Accepts `index` and `shard` for index name and shard number, `from_node` for the node to move the shard from, and `to_node` for the node to move the shard to.</summary>
		public CommandDescriptor Move(CommandMoveAction? value) { _value.Move = value; return this; }
	/// <summary>Move a started shard from one node to another node. Accepts `index` and `shard` for index name and shard number, `from_node` for the node to move the shard from, and `to_node` for the node to move the shard to.</summary>
		public CommandDescriptor Move(Action<CommandMoveActionDescriptor> configure)
	{
		var descriptor = new CommandMoveActionDescriptor();
		configure(descriptor);
		_value.Move = descriptor;
		return this;
	}
	/// <summary>Allocate an unassigned replica shard to a node. Accepts `index` and `shard` for index name and shard number, and `node` to allocate the shard to. Takes allocation deciders into account.</summary>
		public CommandDescriptor AllocateReplica(CommandAllocateReplicaAction? value) { _value.AllocateReplica = value; return this; }
	/// <summary>Allocate an unassigned replica shard to a node. Accepts `index` and `shard` for index name and shard number, and `node` to allocate the shard to. Takes allocation deciders into account.</summary>
		public CommandDescriptor AllocateReplica(Action<CommandAllocateReplicaActionDescriptor> configure)
	{
		var descriptor = new CommandAllocateReplicaActionDescriptor();
		configure(descriptor);
		_value.AllocateReplica = descriptor;
		return this;
	}
	/// <summary>Allocate a primary shard to a node that holds a stale copy. Accepts `index` and `shard` for index name and shard number, and `node` to allocate the shard to. Using this command may lead to data loss for the provided shard id. If a node which has the good copy of the data rejoins the cluster later on, that data will be deleted or overwritten with the data of the stale copy that was forcefully allocated with this command. To ensure that these implications are well-understood, this command requires the flag `accept_data_loss` to be explicitly set to true.</summary>
		public CommandDescriptor AllocateStalePrimary(CommandAllocatePrimaryAction? value) { _value.AllocateStalePrimary = value; return this; }
	/// <summary>Allocate a primary shard to a node that holds a stale copy. Accepts `index` and `shard` for index name and shard number, and `node` to allocate the shard to. Using this command may lead to data loss for the provided shard id. If a node which has the good copy of the data rejoins the cluster later on, that data will be deleted or overwritten with the data of the stale copy that was forcefully allocated with this command. To ensure that these implications are well-understood, this command requires the flag `accept_data_loss` to be explicitly set to true.</summary>
		public CommandDescriptor AllocateStalePrimary(Action<CommandAllocatePrimaryActionDescriptor> configure)
	{
		var descriptor = new CommandAllocatePrimaryActionDescriptor();
		configure(descriptor);
		_value.AllocateStalePrimary = descriptor;
		return this;
	}
	/// <summary>Allocate an empty primary shard to a node. Accepts `index` and `shard` for index name and shard number, and `node` to allocate the shard to. Using this command leads to a complete loss of all data that was indexed into this shard, if it was previously started. If a node which has a copy of the data rejoins the cluster later on, that data will be deleted. To ensure that these implications are well-understood, this command requires the flag `accept_data_loss` to be explicitly set to true.</summary>
		public CommandDescriptor AllocateEmptyPrimary(CommandAllocatePrimaryAction? value) { _value.AllocateEmptyPrimary = value; return this; }
	/// <summary>Allocate an empty primary shard to a node. Accepts `index` and `shard` for index name and shard number, and `node` to allocate the shard to. Using this command leads to a complete loss of all data that was indexed into this shard, if it was previously started. If a node which has a copy of the data rejoins the cluster later on, that data will be deleted. To ensure that these implications are well-understood, this command requires the flag `accept_data_loss` to be explicitly set to true.</summary>
		public CommandDescriptor AllocateEmptyPrimary(Action<CommandAllocatePrimaryActionDescriptor> configure)
	{
		var descriptor = new CommandAllocatePrimaryActionDescriptor();
		configure(descriptor);
		_value.AllocateEmptyPrimary = descriptor;
		return this;
	}

	public static implicit operator Command(CommandDescriptor descriptor) => descriptor._value;
}
